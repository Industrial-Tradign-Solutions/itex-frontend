// §4.6 (sourceNumber = Quotation number) / §4.10 (sourceNumber = Quote Request number)
export type AvailableIpPurchaseOrderOtherCharge = {
  id: string;
  description: string;
  value: number;
  sourceNumber: string;
}
